{{- if include "temporal.enabled" . -}}
{{- include "temporalWorker.serviceaccount" (dict "worker" .Values.temporalJobsService.jobsExtractWorker "serviceAccountHelper" "temporalJobsService.jobsExtractWorker.serviceAccountName" "root" .) -}}
{{- end -}}
