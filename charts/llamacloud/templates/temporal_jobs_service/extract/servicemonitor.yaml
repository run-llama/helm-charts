{{- if include "temporal.enabled" . -}}
{{- include "temporalWorker.servicemonitor" (dict "worker" .Values.temporalJobsService.jobsExtractWorker "serviceAccountHelper" "temporalJobsService.jobsExtractWorker.serviceAccountName" "root" .) -}}
{{- end -}}
