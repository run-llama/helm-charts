{{- if include "temporal.enabled" . -}}
{{- include "temporalWorker.secret" (dict "worker" .Values.temporalJobsService.jobsExtractWorker "serviceAccountHelper" "temporalJobsService.jobsExtractWorker.serviceAccountName" "root" .) -}}
{{- end -}}
