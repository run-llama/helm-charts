{{- if include "temporal.enabled" . -}}
{{- include "temporalWorker.service" (dict "worker" .Values.temporalJobsService.jobsServiceWorker "serviceAccountHelper" "temporalJobsService.jobsServiceWorker.serviceAccountName" "root" .) -}}
{{- end -}}
