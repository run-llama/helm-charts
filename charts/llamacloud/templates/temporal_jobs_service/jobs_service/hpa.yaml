{{- if include "temporal.enabled" . -}}
{{- include "temporalWorker.hpa" (dict "worker" .Values.temporalJobsService.jobsServiceWorker "serviceAccountHelper" "temporalJobsService.jobsServiceWorker.serviceAccountName" "root" .) -}}
{{- end -}}
