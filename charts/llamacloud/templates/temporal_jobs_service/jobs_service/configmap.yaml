{{- if include "temporal.enabled" . -}}
{{- include "temporalWorker.configmap" (dict "worker" .Values.temporalJobsService.jobsServiceWorker "serviceAccountHelper" "temporalJobsService.jobsServiceWorker.serviceAccountName" "root" .) -}}
{{- end -}}
