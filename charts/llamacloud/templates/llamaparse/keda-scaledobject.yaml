{{- if and (.Capabilities.APIVersions.Has "keda.sh/v1alpha1") .Values.llamaParse.enabled .Values.llamaParse.keda.enabled }}
{{- if .Values.llamaParse.autoscaling.enabled }}
{{- fail "Keda configuration (`.Values.llamaParse.keda`) and HPA configurations (`.Values.llamaParse.autoscaling`) cannot be both enabled at the same time!" }}
{{- end }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "llamacloud.fullname" . }}-{{ .Values.llamaParse.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "llamacloud.labels" . | nindent 4 }}
    {{- with .Values.llamaParse.keda.additionalLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- include "llamacloud.annotations" . | nindent 4 }}
    {{- with .Values.llamaParse.keda.additionalAnnotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "llamacloud.fullname" . }}-{{ .Values.llamaParse.name }}
  pollingInterval: {{ .Values.llamaParse.keda.pollingInterval }}
  cooldownPeriod: {{ .Values.llamaParse.keda.cooldownPeriod }}
  initialCooldownPeriod: {{ .Values.llamaParse.keda.initialCooldownPeriod }}
  minReplicaCount: {{ .Values.llamaParse.keda.minReplicaCount }}
  maxReplicaCount: {{ .Values.llamaParse.keda.maxReplicaCount }}
  {{- with .Values.llamaParse.keda.fallback }}
  fallback:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.llamaParse.keda.advanced }}
  advanced:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.llamaParse.keda.triggers }}
  triggers:
    {{- tpl (toYaml .) $ | nindent 4 }}
  {{- else }}
    {{- fail "At least one element in `.Values.llamaParse.keda.triggers` is required!" }}
  {{- end }}
{{- end }}
