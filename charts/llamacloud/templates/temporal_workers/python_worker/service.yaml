{{- if include "temporal.enabled" . -}}
{{- include "temporalWorker.service" (dict "worker" .Values.temporalWorkers.pythonWorker "serviceAccountHelper" "temporalWorkers.pythonWorker.serviceAccountName" "root" .) -}}
{{- end -}}
