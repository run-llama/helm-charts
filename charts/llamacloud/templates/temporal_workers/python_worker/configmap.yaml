{{- if include "temporal.enabled" . -}}
{{- include "temporalWorker.configmap" (dict "worker" .Values.temporalWorkers.pythonWorker "serviceAccountHelper" "temporalWorkers.pythonWorker.serviceAccountName" "root" .) -}}
{{- end -}}
