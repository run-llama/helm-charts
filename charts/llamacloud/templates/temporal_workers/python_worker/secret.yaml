{{- if include "temporal.enabled" . -}}
{{- include "temporalWorker.secret" (dict "worker" .Values.temporalWorkers.pythonWorker "serviceAccountHelper" "temporalWorkers.pythonWorker.serviceAccountName" "root" .) -}}
{{- end -}}
