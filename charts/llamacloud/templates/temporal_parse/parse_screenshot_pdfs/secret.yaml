{{- if include "temporal.enabled" . -}}
{{- include "temporalWorker.secret" (dict "worker" .Values.temporalParse.parseScreenshotPDFs "serviceAccountHelper" "temporalParse.parseScreenshotPDFs.serviceAccountName" "root" .) -}}
{{- end -}}
