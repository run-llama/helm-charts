{{- if include "temporal.enabled" . -}}
{{- include "temporalWorker.service" (dict "worker" .Values.temporalParse.parseScreenshotPDFs "serviceAccountHelper" "temporalParse.parseScreenshotPDFs.serviceAccountName" "root" .) -}}
{{- end -}}
