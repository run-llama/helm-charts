{{- if include "temporal.enabled" . -}}
{{- include "temporalWorker.servicemonitor" (dict "worker" .Values.temporalParse.parseScreenshotPDFs "serviceAccountHelper" "temporalParse.parseScreenshotPDFs.serviceAccountName" "root" .) -}}
{{- end -}}
