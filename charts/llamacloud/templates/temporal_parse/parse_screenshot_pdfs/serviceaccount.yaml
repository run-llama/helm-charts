{{- if include "temporal.enabled" . -}}
{{- include "temporalWorker.serviceaccount" (dict "worker" .Values.temporalParse.parseScreenshotPDFs "serviceAccountHelper" "temporalParse.parseScreenshotPDFs.serviceAccountName" "root" .) -}}
{{- end -}}
