{{- if include "temporal.enabled" . -}}
{{- include "temporalWorker.keda" (dict "worker" .Values.temporalParse.parseDelegate "serviceAccountHelper" "temporalParse.parseDelegate.serviceAccountName" "root" .) -}}
{{- end -}}
