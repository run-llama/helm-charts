{{- if include "temporal.enabled" . -}}
{{- include "temporalWorker.hpa" (dict "worker" .Values.temporalParse.parseDelegate "serviceAccountHelper" "temporalParse.parseDelegate.serviceAccountName" "root" .) -}}
{{- end -}}
