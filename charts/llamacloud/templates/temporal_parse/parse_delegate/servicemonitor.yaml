{{- if include "temporal.enabled" . -}}
{{- include "temporalWorker.servicemonitor" (dict "worker" .Values.temporalParse.parseDelegate "serviceAccountHelper" "temporalParse.parseDelegate.serviceAccountName" "root" .) -}}
{{- end -}}
